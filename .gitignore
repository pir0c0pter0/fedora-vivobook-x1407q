# ISOs are too large for git
*.iso
*.iso.xz
*.iso.xz.part[0-9][0-9]

# Temporary build artifacts
/tmp/
*.bak
.claude/
.worktrees/
docs/superpowers/

# Source tarballs and SRPMs (downloaded for analysis, not part of project)
*.tar.xz
*.tar.gz
*.src.rpm
gtk-4.21.6/
mesa-25.3.6/

# Pre-built binaries (compile from vk_pool_fix.c instead)
vk_pool_fix.so

# Backup dos drivers Windows — grande demais para git (hash no BUILD-STATE.md)
windows-drivers/

# Artefatos de build de módulos kernel e backups de pull
modules/**/*.ko
modules/**/*.o
modules/**/.*.cmd
modules/**/Module.symvers
modules/**/modules.order
modules/**/*.mod
modules/**/*.mod.c
*.pre-pull.local

# Shim de userspace compilado (fonte em modules/qnn-soc-id-fix/)
modules/**/*.so
__pycache__/
*.pyc

# Estado de ferramentas
.claude-flow/
